/** Рост (см) — для формулы процента жира и расхода. */
export const HEIGHT_CM = 185

/** Возраст (лет) — для формулы обмена. */
export const AGE = 32

/**
 * % жира на момент первого взвешивания — точных замеров больше не ведём
 * (только вес), это ориентир по памяти. Дальше % считается не «в лоб» по весу
 * (вес = жир + вода + мышцы), а через дефицит калорий: сколько жира должно
 * было уйти по факту питания, вычитается из стартовой жировой массы.
 */
export const BODY_FAT_START_PCT = 28
