/** Целевое потребление для «идеального темпа» (ккал/день). */
export const DAILY_KCAL_TARGET = 2200

/** Коэффициент активности (сидячая работа + тренировки). */
export const ACTIVITY_FACTOR = 1.4

/** Калорий в 1 кг жира. */
export const KCAL_PER_KG = 7700

/**
 * Окно стартовой адаптации (дней). Первые дни дефицита вес падает в основном
 * за счёт гликогена и связанной с ним воды (~2.7 г воды на 1 г гликогена),
 * а не жира. Данные этого окна не годятся для оценки темпа.
 */
export const WATER_ADAPTATION_DAYS = 10
